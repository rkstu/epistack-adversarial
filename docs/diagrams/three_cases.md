# Three Cases — Different Epistemic Structures

Same pipeline, three different diagnoses:

```mermaid
graph TD
    subgraph COVID ["COVID-19 Origins (Contested)"]
        direction LR
        C_P1[Lab Leak<br/>62 claims] ---|"204 contradicts"| C_P2[Zoonotic<br/>76 claims]
        C_CR[10 live cruxes<br/>top: 0.61]
        C_ST["⚠️ 9 verdicts<br/>performed settling"]
    end

    subgraph LHC ["LHC Black Holes (Settled)"]
        direction LR
        L_P1[Safety Case<br/>consensus] --> |"215 supports"| L_P2[Conclusion:<br/>no danger]
        L_CR[2 cruxes<br/>both low-impact]
        L_ST["✓ No settling<br/>dependency chain intact"]
    end

    subgraph EGGS ["Eggs & Health (Framework Mismatch)"]
        direction LR
        E_P1[Observational<br/>mortality] ---|"11 frames_differently"| E_P2[Mechanistic<br/>LDL/cholesterol]
        E_CR[4 cruxes<br/>all cross-framework]
        E_ST["✓ Not a dispute<br/>different questions"]
    end

    style COVID fill:#fef2f2,stroke:#dc2626
    style LHC fill:#f0fdf4,stroke:#16a34a
    style EGGS fill:#eff6ff,stroke:#2563eb
```

## What Each Case Demonstrates

| Case | Pipeline Diagnosis | Key Evidence |
|------|-------------------|--------------|
| **COVID** | Contested — cruxes unresolved, settling detected | 204 `contradicts`, 78 `frames_differently`, 9 verdicts settling |
| **LHC** | Settled — dependency chain supports conclusion | 215/232 edges are `supports`, minimal contradictions |
| **Eggs** | Framework mismatch — not a factual dispute | 11 `frames_differently` edges, no single load-bearing crux |
