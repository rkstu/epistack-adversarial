# templates

Currently empty. HTML templates are defined inline in `src/epistack/generate_site.py` using Jinja2 string templates.

This directory is reserved for future extraction of templates into standalone `.html` files. The inline approach works well for the current 4 page types (index, position, crux, claim) but would benefit from filesystem templates if more page types are added.
